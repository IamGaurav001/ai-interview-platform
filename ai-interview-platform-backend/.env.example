# ==========================================
# OWASP Security: Environment Configuration
# ==========================================
# IMPORTANT: Never commit the actual .env file to version control
# Keep all sensitive credentials in environment variables
# Rotate API keys regularly (recommended: every 90 days)
# Use different keys for development and production

# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration
# SECURITY: Use strong passwords, enable IP whitelisting, enable authentication
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-interview-platform?retryWrites=true&w=majority

# Redis Configuration
# SECURITY: Use TLS (rediss://) in production, enable authentication
REDIS_URL=redis://your-redis-host:6379
# Or for Redis Cloud/Upstash with TLS:
# REDIS_URL=rediss://default:password@your-redis-host:6379

# Firebase Admin SDK
# SECURITY: Never expose these values client-side
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=your-private-key
FIREBASE_CLIENT_EMAIL=your-client-email

# Google Cloud Text-to-Speech (if using)
# SECURITY: Use service account with minimal required permissions
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# Google Gemini AI
# SECURITY: Multiple keys can be comma-separated for load balancing
# Rotate keys regularly, monitor usage for anomalies
GEMINI_API_KEY=your-gemini-api-key-1,your-gemini-api-key-2

# CORS Origins (comma-separated)
# SECURITY: Only list trusted domains, never use "*" in production
ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173
FRONTEND_URL=https://your-frontend.vercel.app

# JWT Secret (generate a random string)
# SECURITY: Use a strong random string (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this

# Razorpay Payment Gateway
# SECURITY: Never expose key_secret client-side
# Use webhook signature verification for all payment callbacks
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Email Configuration (SMTP)
# SECURITY: Use app-specific passwords, enable 2FA on email account
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Admin Configuration
# SECURITY: Use a dedicated admin email, enable 2FA
ADMIN_EMAIL=admin@yourdomain.com

# Auth Domain (for Firebase email links)
AUTH_DOMAIN=your-project.firebaseapp.com

# ==========================================
# Security Best Practices Checklist:
# ==========================================
# ✓ All sensitive values are in environment variables
# ✓ .env file is in .gitignore
# ✓ Different credentials for dev/staging/production
# ✓ API keys are rotated regularly
# ✓ Strong passwords (min 16 chars, mixed case, numbers, symbols)
# ✓ Database has IP whitelisting enabled
# ✓ Redis has authentication enabled
# ✓ HTTPS/TLS enabled for all external connections
# ✓ Rate limiting configured on all endpoints
# ✓ Input validation on all user inputs
# ✓ CORS restricted to trusted domains only
# ==========================================
